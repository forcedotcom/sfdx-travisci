branches:
  only:
  - travisdev
  - travismaster
sudo: true

before_install:
- openssl aes-256-cbc -K $encrypted_807396767087_key -iv $encrypted_807396767087_iv
  -in assets/server.key.enc -out assets/server.key -d
- export CLIURL=https://developer.salesforce.com/media/salesforce-cli/sfdx-linux-amd64.tar.xz  
- export SFDX_AUTOUPDATE_DISABLE=false
- export SFDX_USE_GENERIC_UNIX_KEYCHAIN=true
- export SFDX_DOMAIN_RETRY=300
- export SFDX_DISABLE_APP_HUB=true
- export SFDX_LOG_LEVEL=DEBUG
- mkdir sfdx
- wget -qO- $CLIURL | tar xJ -C sfdx --strip-components 1
- "./sfdx/install"
- export PATH=./sfdx/$(pwd):$PATH

before_script:
- export ROOTDIR=force-app/main/default/
- export DEPLOYDIR=deploy
- export TESTLEVEL=RunLocalTests
- sfdx --version
- sfdx plugins --core
#Authorize DevHub
- sfdx force:auth:jwt:grant --clientid $CONSUMERKEY --jwtkeyfile assets/server.key --username $USERNAME --setdefaultdevhubusername -a HubOrg

script:
  #Create Scratch Org
- sfdx force:org:create -v HubOrg -s -f config/project-scratch-def.json -a ciorg --wait 10
- sfdx force:org:display -u ciorg
  #Push Source Scratch Orgs
- sfdx force:source:push -u ciorg
  #Run Unit Tests on Scratch Org
- sfdx force:apex:test:run -u ciorg --wait 10 --resultformat tap --codecoverage --testlevel $TESTLEVEL
  #Delete Scratch Org
- sfdx force:org:delete -u ciorg -p
  #Convert DX to metadata format into new folder based on DEPLOYDIR variable
- sfdx force:source:convert -r $ROOTDIR -d $DEPLOYDIR
  #Authorize Target Deployment Org
- sfdx force:auth:jwt:grant --clientid $UATCONSUMERKEY --jwtkeyfile assets/server.key --username $UATUSERNAME -a UAT
  #Deploy Target Deployment Org
- sfdx force:mdapi:deploy -w -1 -d $DEPLOYDIR -u UAT --testlevel $TESTLEVEL
  #Example shows how to run a check only deploy.
  #- sfdx force:mdapi:deploy --checkonly -w -1 -d $DEPLOYDIR -u UAT --testlevel $TESTLEVEL