trigger:
  batch: "true"
  branches:
    include:
      - master

pr:
  autoCancel: "true"
  branches:
    include:
      - master
      
jobs:

- job: ProdDeploy
  displayName: Production Deploy
  condition: and(succeeded(), eq(variables['Build.SourceBranch'], 'refs/heads/master'))
  steps:
  - powershell:

      $files = $(git diff HEAD HEAD~ --name-only);
      $temp = $files -split ' ';
      $count = $temp.Length;
      Write-Output "Total changed $count files";
      For ($i=0; $i -lt $temp.Length; $i++){
        $nameOfChangedFile = $temp[$i];
        echo "Changed File - $nameOfChangedFile";
      }

      $EmailTo = "vj.vivekjoshi@gmail.com";
      $EmailFrom = "vj.vivekjoshi@gmail.com";
      $Subject = "Test";
      $Body = "Test Body";
      $SMTPServer = "mail"; 
      $SMTPMessage = New-Object System.Net.Mail.MailMessage($EmailFrom,$EmailTo,$Subject,$Body);
      $SMTPClient = New-Object Net.Mail.SmtpClient($SmtpServer, 587); 
      $SMTPClient.EnableSsl =$False; 
      $SMTPClient.Credentials = New-Object System.Net.NetworkCredential("vivek@infoglen.com", "Info#Glen1"); 
      $SMTPClient.Send($SMTPMessage);