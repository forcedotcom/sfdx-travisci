trigger:
  batch: "true"
  branches:
    include:
      - master

pr:
  autoCancel: "true"
  branches:
    include:
      - master
      
jobs:

- job: ProdDeploy
  displayName: Production Deploy
  condition: and(succeeded(), eq(variables['Build.SourceBranch'], 'refs/heads/master'))
  steps:
  - powershell:

      $files = $(git diff HEAD HEAD~ --name-only);
      $temp = $files -split ' ';
      $count = $temp.Length;
      Write-Output "Total changed $count files";
      For ($i=0; $i -lt $temp.Length; $i++){
        $nameOfChangedFile = $temp[$i];
        echo "Changed File - $nameOfChangedFile";
      }

      $EmailFrom = "vivekjoshiji12@gmail.com";
      $recipient = "vivek@infoglen.com";
      $EmailSubj = "Subject";
      $EmailBody = "Hello Vivek";
      $SMTPServer = "smtp.gmail.com";
      $SMTPClient = New-Object Net.Mail.SMTPClient($SmtpServer, 587);
      $SMTPClient.EnableSSL = $true;
      $SMTPClient.Credentials = New-Object System.Net.NetworkCredential("vivekjoshiji12", "test@#$*1");
      $emailMessage = New-Object System.Net.Mail.MailMessage;
      $emailMessage.From = $EmailFrom;
      $emailMessage.To.Add($recipient);
      $emailMessage.Subject = $EmailSubj;
      $emailMessage.Body = $EmailBody;
      $SMTPClient.Send($emailMessage);