Skip to content
This repository
Search
Pull requests
Issues
Marketplace
Explore
 @snw832
 Sign out
 Watch 0
  Star 0  Fork 1,259 snw832/sfdx-travisci
forked from forcedotcom/sfdx-travisci
 Code  Pull requests 0  Projects 0  Wiki  Insights  Settings
Branch: master Find file Copy pathsfdx-travisci/.travis.yml
53b9956  7 minutes ago
@snw832 snw832 Update .travis.yml
3 contributors @wadewegner @dcarroll @snw832
RawBlameHistory     
27 lines (23 sloc)  938 Bytes
sudo: true
dist: trusty 
cache: false

env:
- URL=https://developer.salesforce.com/media/salesforce-cli/sfdx-linux-amd64.tar.xz

before_install:
# delete the following line before encrypting your server.key.
- openssl -sha256 -K $encrypted_f42060fe945a_key -iv $encrypted_f42060fe945a_iv -in assets/server.key.enc -out assets/server.key -d
- export SFDX_AUTOUPDATE_DISABLE=true
- export SFDX_USE_GENERIC_UNIX_KEYCHAIN=true
- export SFDX_DOMAIN_RETRY=300
- wget -qO- $URL | tar xJf -
- "./sfdx/install"
- export PATH=./sfdx/$(pwd):$PATH
- sfdx update
- sfdx --version
- sfdx plugins --core
- sfdx force:auth:jwt:grant --clientid $CONSUMERKEY --jwtkeyfile assets/server.key --username $USERNAME --setdefaultdevhubusername -a HubOrg

script:
- sfdx force:org:create -v HubOrg -s -f config/project-scratch-def.json -a ciorg
- sfdx force:source:push -u ciorg
- sfdx force:apex:test:run -u ciorg -c -r human
- sfdx force:org:delete -u ciorg -p
